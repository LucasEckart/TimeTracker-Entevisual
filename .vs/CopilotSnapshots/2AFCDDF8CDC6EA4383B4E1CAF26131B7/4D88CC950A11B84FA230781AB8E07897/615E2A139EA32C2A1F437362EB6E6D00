@model IEnumerable<TipoActividad>

<link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />

<div class="tt-shell mt-4">
    <div class="tt-card p-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h3 class="mb-0">Tipos de actividad</h3>
                <p class="tt-text-2 mb-0">Listado de tipos</p>
            </div>
            <div>
                <a asp-action="Create" class="btn btn-primary">Nuevo</a>
            </div>
        </div>

        <div class="table-responsive">
            <table id="tipoActividadTable" class="display table table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Descripción</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Descripcion</td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info me-1">Ver</a>
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-secondary me-1">Editar</a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Eliminar</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#tipoActividadTable').DataTable({
                language: { url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json' },
                columnDefs: [{ orderable: false, targets: 2 }]
            });
        });
    </script>
}
